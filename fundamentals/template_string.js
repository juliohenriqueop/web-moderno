const name = 'Rebeca'

const message = `
    Hello,
    ${name}!
`
console.log(message)

const expression = `1 + 1 = ${1 + 1}`
console.log(expression)

const warning = `${'warning'.toUpperCase()}!`
console.log(warning)